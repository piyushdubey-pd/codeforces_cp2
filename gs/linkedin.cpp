#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int> batchSize;
    vector<int> numTasks;
    int n;
    vector<int> processTime;

    cin>>n;
    for(int i=0;i<n;i++){
    int x;
    cin>>x;
    batchSize.push_back(x);
    }
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        processTime.push_back(x);
    }
    for(int i=0;i<n;i++){
        int x;
        cin>>x;
        numTasks.push_back(x);
    }
    long long final_time=0;
    for(int i=0;i<n;i++){
        
    }
}